console.log(isLandscape(900,500));

function isLandscape(width,height){
    return(width>height);
}
//  instead of writing a conditional statement we can  simply return the value using a comparision opertor