const numbers = [1,100];
console.log(numbers.indexOf(100));//this gives what is in that index

//  console.log(numbers.includes(6));// this gives whether the array includes the element or not i.e, true or false

// console.log(numbers.lastIndexOf(4))//gives the number of the index from last

