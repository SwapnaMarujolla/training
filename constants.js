//  let intrestRate ;
//   intrestRate = 1;
// console.log('intrestRate',intrestRate);



let radius = 5;
const pi = 3.14;
area = pi * radius * radius;
 // radius = 6;the new value of the radius will be considered as 6
// pi = 5;
// console.log(area);
// here we can change the value of the variable but the connstant i.e, the value of pi remains the same.
// in const once we initialize a value that cannot be changed.
// if we tey to change a constant value it will give an error Assignment to a constant value.
// if instead of const if we declare the pi value as let it will accept and gives some particular value

