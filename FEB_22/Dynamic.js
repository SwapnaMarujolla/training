const circle = {
    radius:1,
    
};

    

//circle = {}; by assigning this we get a new variable

circle.color = 'Yellow';

circle.draw =function () {}
// 
console.log(circle);

// we have used a const keyword to define a new constant
// that means we cannot reassign a variable
// but we can change objects by adding or removing proprety